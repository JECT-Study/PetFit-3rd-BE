spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/petfit
    username: petfit
    password: ject-3rd-petfit

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  kakao:
    auth:
      admin: 74f521389d656a1e56c68d4223ccf874
      client: 84d27ea7be4ba339a95254c674c3c972
      redirect: http://localhost:8080/auth/kakao/login

  jwt:
    issuer: cms3604@naver.com
    secret: ject-3rd-petfit
    access-token-time: 3600000
    refresh-token-validity-seconds: 604800000

app:
  front:
    local: http://localhost:5173
    vercel: https://pet-fit-3rd-fe.vercel.app
    domain: http://localhost:5173
  backend:
    domain: http://localhost:8080
  swagger:
    server: 로컬 서버
    description: 펫핏 API 명세서입니다

springdoc:
  api-docs:
    path: /api-docs

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

openai:
  api-key: ${OPENAI_API_KEY}